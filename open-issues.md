# Open Issues — Eugene (AI IT & Engineering Assistant)

_The `OI-<n>` table. A resolved issue gets a `Resolved` line, never a deletion. See `CLAUDE.md` §4 and §6._

| OI | Opened | Status | Issue |
|---|---|---|---|
| OI-1 | 2026-09-12 | **Resolved 2026-09-13** | **Google Workspace tenant shape.** The paid Google Workspace companies (Fishbone Construction, Properties, Commercial Properties, Waste, **Amfa Furniture**) — are they in **one tenant with multiple domains**, or **separate subscriptions/tenants**? Fishbone Holdings and the SSAS are on 1&1 Webmail. This decides whether consolidating the 1&1 domains is "add secondary domains to the existing tenant" (simple) or "consolidate tenants first" (larger). **Resolved (owner, 2026-09-13):** they are **separate subscriptions** — Construction, Properties, Waste and Amfa each have their own paid Workspace; **Commercial Properties has no separate subscription and rides inside Properties'** (`commercial@fishboneproperties.co.uk`). So **four subscriptions cover five companies**; Holdings + SSAS remain on 1&1. Because a domain can live in only one Workspace account, consolidation is a **per-domain migration**, not a secondary-domain add — recorded in `Infra-Inventory/Workspace-and-Email-Inventory.md`, and the consolidation-path choice is now **OI-4**. |
| OI-2 | 2026-09-12 | Open | **Gmail connector delegated-mailbox capability.** Confirm whether the Gmail connector can read a Google Group / delegated / shared mailbox, or only the connected account's own primary mailbox. Decides whether per-company `info@` works via group membership or must be forwarded into one ops mailbox. Directly relates to **Peter OI-5** (the connector currently reaches minda@'s own mailbox, not a dedicated info@). A ~5-minute test in Phase 0. Decision/test: Minda + Eugene. |
| OI-3 | 2026-09-12 | Open | **First-runbooks priority.** Proposed order: (a) Workspace multi-domain + ops account (unblocks dedicated agent accounts group-wide), (b) Companies House egress allowlist (resolves Peter OI-6, unblocks Peter beat 2b), (c) dedicated `info@` groups (resolves Peter OI-5). Confirm or reorder. Decision: Minda. **Note (2026-09-13):** now that OI-1 shows the Workspaces are separate subscriptions, item (a) splits — the *ops account + dedicated `info@`* work can proceed per-subscription without a migration (path B/C of OI-4), while a *full domain consolidation* is a larger separate project. |
| OI-4 | 2026-09-13 | **Resolved 2026-09-13** | **Email-hosting consolidation path** (arises from OI-1). Given separate subscriptions, choose between full consolidation, standardise-in-place, or hybrid. **Resolved (owner, 2026-09-13):** a **partial consolidation into a Construction hub** — fold **Holdings** and **SSAS** (off 1&1) and **Waste** (dormant, its own Workspace) into **Construction** as secondary domains; **keep Properties (+ Commercial) as its own tenant** (separate legal company, governance line); **keep Amfa standalone for sale-readiness** (group OI-13). Target = three tenants (Construction hub, Properties, Amfa). Clarified that a secondary domain keeps its own email/website/brand — consolidation only merges admin + billing — so the "separate website" was not a reason to keep Amfa separate; sale-readiness is. Runbook drafted: `Runbooks/Runbook-Workspace-Consolidation-into-Construction.md` (v0.1, pending the §2 prerequisites Minda reads from the consoles). **Update 2026-09-15:** owner reports Holdings already migrated under Construction; **MX verified same day** (Minda's own 1&1 DNS-panel screenshot, Eugene's DNS tooling stayed network-blocked all session) — mail confirmed routing to Google. Stale-SPF loose end flagged (still IONOS, low urgency). SSAS has no domain/Workspace of its own, so it drops out — only **Waste** remains to migrate, and its §2 prerequisites are now fully answered. Runbook now v0.3. |
| OI-5 | 2026-09-15 | Open (advisory, not blocking) | **Super-admin identity is a shared business mailbox.** Owner-reported 2026-09-15: each Workspace subscription's super-admin login is the company's shared business inbox itself (`info@fishboneconstruction.co.uk`, `info@fishboneproperties.co.uk`, `sales@` for Waste, `info@` for Amfa) rather than a dedicated named admin account. This means anyone who can read that inbox has full Workspace admin rights, and there's no separate break-glass admin identity distinct from day-to-day business mail. Not blocking the Waste migration or anything else — flagged for the owner's awareness; Eugene takes no action here (guide-only, charter §3). Decision (create a dedicated named super-admin per subscription, or accept as-is): Minda. |
| OI-6 | 2026-09-15 | Open (task, not blocking) | **Holdings' SPF record still points at IONOS, not Google.** Found 2026-09-15 verifying the Holdings MX migration (Minda's 1&1 DNS-panel screenshot): `fishboneholdings.co.uk`'s SPF TXT record still references IONOS's mail servers (`_spf-eu.ionos.com`), not Google's, even though MX now correctly routes incoming mail to Google Workspace. SPF governs whether *outgoing* mail sent through Google's servers is authenticated as legitimate — left as-is, mail sent from this domain via Google Workspace risks failing SPF at the receiving end (spam-folder risk, or outright rejection by strict recipients). **Fix:** add `include:_spf.google.com` to the domain's SPF TXT record in the 1&1/IONOS DNS panel (Domains & SSL → `fishboneholdings.co.uk` → DNS). Not urgent — matters once the domain actively sends mail via Google — but worth doing. Action: Minda (DNS record edit; Eugene is guide-only for live DNS, charter §3). Verification once done: Eugene re-checks the SPF record (subject to the same DNS-egress limits noted under OI-4/Holdings) or Minda reports the updated value back. |
| OI-7 | 2026-09-17 | Open (task, **critical** — company phone system, no backup, no support cover) | **HP ProLiant server runs FusionPBX (the whole company's phone system, both sites) — with no backup and no support contract.** Fishbone HQ (Unit 30–31, Point Pleasant Industrial Estate, Wallsend NE28 6HA) has a rack-mounted HP ProLiant server (photographed 2026-09-17 — 2U, SFF chassis, Intel Xeon, iLO cabled to switch port 17 per the site's Fishbone Network Diagram). **Confirmed 2026-09-17 (Minda):** it hosts **FusionPBX** (self-hosted FreeSWITCH-based PBX, internal IP `10.224.13.9`) — the phone system both the HQ Yealink T48S handsets *and* the Beverley Place site register to (the latter over the airFiber bridge / IPsec VPN). **Also confirmed: no backup exists anywhere, and no support/warranty contract is held on this server.** This is now a genuine single point of failure for company-wide telephony: if this box fails, phones go down at both sites simultaneously, with no backup to restore the PBX config/extensions/CDRs/recordings from and no support contract to fall back on. Eugene has no remote/credentialed access to check further himself (charter §3 — never holds credentials), so this stays guide-only. **Access situation clarified 2026-09-17:** this is **not a total lockout** — Minda already has full **FusionPBX application-level access** (the web admin GUI, `10.224.13.9`, logged in as `admin`) for day-to-day phone management (extensions, CDRs, voicemail). What's missing is **OS-level access** (no SSH/console login credentials) — the guy who helped build the network likely has these, or they may recoverable via an HPE iLO default-password tag on the chassis if iLO was never reconfigured. **Confirmed via console observation only (no login attempted): the OS is Debian 12** — a standard, officially-supported FusionPBX platform, not an obscure appliance, so once OS access is sorted the backup work itself is a well-documented, unexciting sysadmin job. **Built-in backup ruled out, 2026-09-17:** Minda checked Advanced, Applications, and the Modules page (55 FreeSWITCH modules listed) in the FusionPBX admin GUI — no "Backup" app or module anywhere. This install has **no built-in backup feature at all**; that avenue is closed. **The critical path is now singular: get OS-level access.** (1) **Get OS-level access** — ask the contractor who helped build this first; failing that, check the ProLiant chassis for an HPE iLO default-password tag (pull-out asset label or sticker, common on Gen9/Gen10). OS confirmed as **Debian 12** via console observation, so once in, the backup itself (a scheduled FreeSWITCH/FusionPBX database dump plus the relevant config and recording directories, sent off that physical box) is a standard, well-documented job — Eugene will draft the exact runbook once there's shell access to confirm the real layout (bare-metal vs a hypervisor, where the DB/recordings actually live). (2) Once backed up, decide whether to take out a support contract on this hardware or treat this as the trigger to virtualise/replace it, given it's clearly business-critical rather than experimental. Action: Minda for access; Eugene drafts the backup runbook the moment there's OS-level visibility. |
| OI-8 | 2026-09-17 | Open (task, **critical, live outage**) | **SIP trunk migration (WebMate/SIP Trunk Solutions, ticket T02530-15072026) — company phones currently down.** A pending SIP trunk migration for DDIs `+441916052945` through `948` turned out to already be blocking live service — Minda found IP Telephony not working because the new gateway hadn't been added to FusionPBX yet. **Progress made 2026-09-17:** new Gateway `WebMate-SIPTrunk` added and registering successfully (`REGED`) against `sipreg.siptrunk.solutions`; existing inbound Destinations for all 4 DDIs confirmed already correctly routed (1001+Ring Group "1001 Boss", 1003, 1005, 1006); extension 1001 confirmed registered; Ring Group "Boss" config confirmed correct (Simultaneous, 1001+1002). **Root cause found:** a test call to 01916052945 produced zero CDR entry at all (most recent prior CDR entry was 21 Jul 2026, ~2 months earlier) — meaning inbound calls never reach FreeSWITCH's dialplan. FusionPBX's **"providers" Access Control** (Advanced → Access Controls) is **default-deny with exactly one allowed entry, `93.95.124.106/32`** (almost certainly the old provider's IP) — the new provider's traffic has no matching allow entry and is being silently dropped at the ACL, before it ever reaches the CDR. **Eugene tried to find the new provider's signaling/media IP ranges independently** (web search, direct fetch) — found nothing; `siptrunk.solutions` doesn't even resolve publicly (no public site/docs), and search results kept surfacing an unrelated same-sounding company (`siptrunk.com`, a different US business) that must **not** be used. **Action taken:** drafted and sent (via Minda) a reply to ticket T02530-15072026 asking WebMate for the exact SIP signaling + media server IP ranges and port info to whitelist. **Path B tried, 2026-09-17 — did not fix it:** Minda flipped the "providers" ACL's Default from `deny` to `allow`, then used the SIP Status page's **Reload ACL** button to push the change live. Retested the call to 01916052945: **still nothing reaching the Yealink, still zero CDR entry.** This rules out the FusionPBX ACL as the (sole) blocker — with Default wide open and the ACL freshly reloaded into the running profile, an ACL-only problem should have let the call through. **Suspicion moved to the Cisco ASA firewall pair** sitting in front of the ProLiant: if its inbound access-list/NAT rule for SIP (UDP 5060) and RTP media is locked to the old provider's IP `93.95.124.106` only, the new provider's packets would be dropped at the network perimeter before ever reaching FusionPBX — same symptom (zero CDR) either way. **New blocker found, 2026-09-17: Minda does not hold ASA admin credentials herself** (the contractor who helped build the network manages that device) — she has asked him for access. So the ASA-side check/fix is now blocked on a third party responding, in parallel with WebMate's ticket reply on the IP ranges — **two external dependencies, phones still down.** **Reload XML also tried, 2026-09-17 — no change.** With both Reload ACL and the broader Reload XML tried and neither restoring service, the FusionPBX side is now fully exhausted — every in-house check available without OS/device-level access has been run. **Confirms the block sits outside FusionPBX**, almost certainly at the ASA. **Status: fully blocked on two external replies** — WebMate (ticket T02530-15072026, real SIP IP ranges) and the contractor (ASA admin access) — no further troubleshooting possible from the FusionPBX GUI alone. Action: Minda (chase both); Eugene available to help interpret either reply and word the exact ASA rule once there's access. No credential ever written down by Eugene throughout. |
