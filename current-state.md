# Current State — Eugene (AI IT & Engineering Assistant)

_Standing control file. Overwritten by archive-then-recreate at the end of any session that changes it — it holds the present snapshot, not history. Per-session history lives in `change-log/`. Companion standing files: `open-issues.md`, `external-source-register.md`, `processed-items-ledger.md`. See `CLAUDE.md` §4._

| Field | Value |
|---|---|
| Last session | **2026-09-16 — AI Workforce Hub charter §2e reconciled; Smartsheet added as a real connector.** Minda asked Eugene to look up two Hub item codes, `AWT0012` and `HL-0007`, from the group's Smartsheet-backed AI Workforce Hub (run by Alex, the group's housekeeping steward) — a system Eugene had no prior visibility into. Found: **AWT-0012** (assigned to Eugene, Blocked) flagged that `CLAUDE.md` §2e (added 2026-09-14 on `main`, not yet pulled into Eugene's working branch) describes Eugene reading/updating the Hub, but his documented Connectors list only named Drive/GitHub/Web — no Smartsheet — so the charter described a capability he couldn't show he had. Eugene confirmed via direct tool use that **this interactive session does have live Smartsheet access**, narrowing (not resolving) the gap, and reported that fact back onto both AWT-0012 and the linked Help & Lessons row (`HL-0007`) — deliberately not deciding between the two options on the table (add a connector vs. route through Alex), since that was Minda's call. Also found **HL-0007 was a reference collision**: two unrelated rows shared that number (Eugene's §2e question and Alex's separate Fishbone-Group git-default-branch question). At Minda's instruction, renumbered Eugene's row to **HL-0008**, keeping HL-0007 for Alex's (already cross-referenced elsewhere). **While writing that update, discovered the underlying question had already been resolved concurrently** (by another session/Minda): the Smartsheet connector is confirmed **account-wide for every employee** — never actually missing — the observed gap was session-scoping (same pattern already seen with GitHub, AWT-0003, and `Artifact.publish`); the Hub's own Roster was corrected; AWT-0012/HL-0008 marked Done/Answered. **Eugene then pulled `main` into his working branch** (clean merge, no conflicts — `main` had only touched `CLAUDE.md`) to get the real §2e text, and **reconciled his own KB to match**: `CLAUDE.md`'s Connectors list now names Smartsheet (account-wide, session-scoped, per §2e), `current-state.md`'s Connectors and Beats rows updated to match (added beat 2e). No live-system change beyond the two Smartsheet-row edits Minda explicitly authorised; the charter/current-state edits are Eugene's own KB, within his normal remit. **Then Minda asked Eugene to look into HL-0007 itself** (the git-branch-naming question kept by Alex): attached `minda-ui/Fishbone-Group`, investigated via GitHub API tools (branches, PRs, Actions workflows, org-wide code search for the branch name) and found nothing depends on the odd default-branch name — safe to rename, no blast radius. Minda confirmed: rename to `main`. **Hit a real tool gap on execution** — Eugene's GitHub toolset has no rename-branch/set-default-branch action, only content-level operations — so rather than improvise a messier workaround, reported the limitation and handed Minda the exact one-click manual step (Settings → Branches → rename), which auto-moves the default pointer since the renamed branch is already HEAD. HL-0007 updated to **Answered** (not Resolved — the click is still Minda's). See `change-log/change-log-2026-09-16-hub-investigation-charter-reconcile.md`.

**2026-09-15 — §2 prerequisites partly answered; Holdings reported migrated, SSAS dropped, OI-5 raised, runbook now v0.3.** Minda answered the §2 checklist as the session went: super-admin logins are each subscription's shared business mailbox itself (Construction `info@`, Properties `info@`, Waste `sales@`, Amfa `info@` — flagged as **OI-5**, advisory/not blocking); Construction hub is **Business Standard** with 3 spare licences; **Holdings has already been fully migrated** under Construction as a secondary domain (`fishboneholdings.co.uk`); **SSAS has no domain or Workspace of its own** and drops out of the plan entirely; Waste's domain confirmed `fishbonewaste.co.uk`, Amfa's confirmed `amfa.uk`. Eugene tried to independently verify the Holdings migration via DNS (no `dig`/`nslookup`, no `dnspython`, DNS-over-HTTPS to `dns.google` rejected by the session's egress proxy — same restriction as Peter OI-6) and **could not** — recorded as owner-reported, not Eugene-verified, rather than silently accepted. Bumped `Runbooks/Runbook-Workspace-Consolidation-into-Construction.md` to **v0.3**: §1 target architecture updated (Holdings done, SSAS removed), §3 rewritten as a Holdings verification checklist, §8 narrowed to **Waste is the only remaining track**, still blocked on Waste's DNS control and mailbox/mail-volume. Updated `open-issues.md` (OI-4 note added; **OI-5 opened**) and `Infra-Inventory/Workspace-and-Email-Inventory.md` (Holdings/SSAS/Waste/Amfa rows, super-admin identity table, target architecture, "to confirm next"). Then, same session, Minda supplied §2.4 (DNS control): **Minda holds registrar access for both
`fishboneholdings.co.uk` and `fishbonewaste.co.uk`** (SSAS n/a) — recorded in the runbook and
inventory. **This closes every §2 item for Waste except mailbox count/mail volume (§2.5), now the
only thing blocking §4.** Then Minda supplied §2.5 too: Waste has **2 mailboxes** — `info@` and
`sales@` — plus an explicit precaution to **archive both mailboxes' historical mail/Drive data before
migrating**, not just re-point and move on. **§2 is now fully answered — nothing left blocking §4 on
prerequisites.** Rewrote runbook §4 step 1 from an optional "inventory + export" into a required
archive-first step, and flagged two open decisions rather than assuming: (a) where the archive should
live (Eugene has no visibility into a Waste KB, if one exists), and (b) whether `info@`/`sales@` stay
live under Construction after migrating or the archive alone is sufficient. **Then Minda shared a
screenshot of `fishboneholdings.co.uk`'s DNS records from the 1&1 panel — Eugene's own DNS tooling
stayed blocked all session, so read it directly: MX points to `smtp.google.com`, confirming the
Holdings migration is genuinely done (closes the outstanding verification).** Also found in the same
screenshot, flagged as a follow-up (not urgent): the domain's **SPF TXT record still references
IONOS**, not Google — an outbound-deliverability risk once this domain sends mail via Google Workspace;
fix is adding `include:_spf.google.com` in the 1&1 panel. Per the runbook's own rule, recorded the
findings as plain text, not the literal DNS values, and did not store the screenshot. **At Minda's
request, formally tracked this as OI-6** (open, task, not blocking) in `open-issues.md`, with the
runbook and inventory now cross-referencing it instead of carrying the finding only as prose. **Then
Minda decided §4 step 2: `info@fishbonewaste.co.uk` and `sales@fishbonewaste.co.uk` will stay live
under Construction post-migration**, in addition to the pre-migration archive — the archive precaution
was about preserving history, not letting the addresses go dormant. **Then Minda confirmed Waste has
its own KB** — Eugene located it via Drive search: `Fishbone Waste Ltd - Knowledge Base` (folder id
`1LMVTPw4YFw9OmW7GcTjaDEfXqCIjp1ZJ`), standard group KB shape, with a `Raw/` folder already holding a
hand-off precedent and the group's document-numbering/filing policy. **The archive destination is now
settled: `Raw/` (id `1TlNINqtx8JU1Qe6152uqhEPZEvt7JN_C`), per the group §7a hand-off rule.** Minda also
confirmed `sales@`'s super-admin role (OI-5) resolves itself at cancellation (§4 step 5) — no carry-over
decision needed. **§4 is now fully decided end-to-end — ready for Minda to execute.** Rewrote the
runbook's status header and bumped it to **v0.4** ("ready to execute"); registered the Waste KB as
**SRC-9** in `external-source-register.md`. **Then Minda asked for §4 laid out as a standalone
checklist.** Produced `Runbooks/Runbook-Waste-Migration-Execution-Checklist.md` (v1.0) — nine ordered
phases (archive → confirm Construction headroom → free the domain from Waste's Workspace → add to
Construction → recreate mailboxes → optional mail restore → cut MX+SPF together → cancel subscription →
verify). **Flagged a genuine open question in Phase C:** whether `fishbonewaste.co.uk` is primary or
secondary inside Waste's own Workspace — Holdings' migration was the easy case (secondary domain off
1&1); a primary domain generally can't just be "removed," usually requiring full subscription
cancellation with a possible release delay. Presented as a console checkpoint for Minda to verify, not
asserted. Cross-referenced from the main runbook §4. **Then Minda confirmed `fishbonewaste.co.uk` is
DNS-hosted at 1&1, not Google Domains** — same as Holdings, no registrar-transfer complication, though
this is a separate fact from the still-open primary/secondary question (a Workspace account setting,
independent of DNS location) — checklist updated to keep the two distinct. **Then a screenshot of the
IONOS DNS panel revealed something bigger: the domain's nameservers actually point at WordPress**, not
IONOS — IONOS's own panel is inactive/stored-only while this holds (explains why it shows apparently-
correct Google MX records despite being non-authoritative; the real DNS serving mail today is at
WordPress). **Rewrote checklist Phases D and G to redirect the Google verification TXT and the MX/SPF
cutover to WordPress's own DNS dashboard instead of IONOS**, and added a warning not to reset
nameservers back to IONOS as a shortcut (risks the live website). Inventory's Waste row updated with
the same finding. See `change-log/change-log-2026-09-15-holdings-ssas-oi5-runbook-v0.3.md` and
`change-log/change-log-2026-09-15-waste-execution-checklist.md`.

**2026-09-15 — consolidation runbook bumped to v0.2.** Minda asked to reopen `Runbooks/Runbook-Workspace-Consolidation-into-Construction.md`; found it stale (§5 info@/ops account and §6 Companies House allowlist were still written as pending, though both closed later on 2026-09-13). Updated to v0.2: §5 and §6 marked done with cross-references to the runbooks that now own that detail (`Runbook-Dedicated-info-Inbox-and-Ops-Account.md` v0.2, `Runbook-Email-Evidence-Retention-Google-Vault.md` v0.2, `Runbook-Companies-House-Access-for-Peter.md` v0.2), and §8's suggested order tightened to just the Holdings pilot next. Doc-only edit, no live-system change; still blocked on §2 prerequisites. See `change-log/change-log-2026-09-15-consolidation-runbook-v0.2.md`.

**2026-09-13 — OI-1 and OI-4 resolved; consolidation runbook drafted.** Minda confirmed the paid Google Workspaces are **separate subscriptions** (Construction, Properties, Waste, Amfa), Commercial riding inside Properties'; Holdings + SSAS on 1&1 (**OI-1 resolved**). Owner then chose the **consolidation path (OI-4 resolved):** a **Construction hub** — fold **Holdings + SSAS** (off 1&1) and **Waste** (dormant) into Construction as secondary domains; **keep Properties (+ Commercial) as its own tenant**; **keep Amfa standalone (sale-ready, group OI-13)**. Clarified that a secondary domain keeps its own email/website/brand (the separate website was not a reason to keep Amfa out; sale-readiness is). Drafted `Runbooks/Runbook-Workspace-Consolidation-into-Construction.md` (v0.1) — two tracks (1&1→Construction for Holdings/SSAS; Waste Workspace→Construction) + the dedicated `info@`/ops-account setup (Peter OI-5) and the Companies House allowlist (Peter OI-6); pending the §2 prerequisites Minda reads from the consoles. Inventory updated. Then, at the owner's request, split the Companies House allowlist out into its **own standalone runbook** — `Runbooks/Runbook-Companies-House-Access-for-Peter.md` (v0.1) — so Peter's Companies House beat can be unblocked immediately, independent of the email migration: allow the four gov.uk domains for Peter's routine environment + issue a free Companies House REST API key to the ops account (stored as an environment secret; Eugene never holds it) + point Peter's beat-2b prompt at the API. Also drafted the **dedicated `info@` + scoped ops-account runbook** — `Runbooks/Runbook-Dedicated-info-Inbox-and-Ops-Account.md` (v0.1, resolves Peter OI-5): a least-privilege 2FA ops/agent account (the workforce's single identity + home for the CH key and future secrets) + a dedicated `info@` business inbox Peter reads instead of minda@'s personal mailbox; gated by the OI-2 test (Group/shared-mailbox readable by the connector → free Group, else real mailbox or forward into the ops account). Then, **later 2026-09-13, Minda created the `ops@fishboneconstruction.co.uk` account** (least-privilege, 2FA) and **repointed Peter's Gmail connector to it** — Eugene verified (read-only) the connector now reads `ops@`, not minda@. Also confirmed `info@` is **its own separate Workspace user/mailbox** (not an alias), and that `info@` mail does **not** yet reach `ops@` (`deliveredto:info@` in ops@ = zero). Updated the runbook to **v0.2** with the concrete Construction routing and recorded the identity accounts in the inventory. **Later 2026-09-13, Minda set `info@` to forward into `ops@` and Eugene verified it (read-only): a test to `info@` arrived in `ops@`, ops@ shows no personal mail → Peter OI-5 closed.** (One snag fixed en route: forwarding was initially left on "Disable forwarding".) Peter's KB updated (OI-5 resolved; retire the to/cc proxy; read `ops@` directly). Owner then **also added a `minda@ → ops@` forward** (minda@ ~95% business), so Peter gets the business mail that comes to Minda directly, not only `info@`. **Settled inbox design recorded** across the KBs: `ops@` = forwarded `info@` + business `minda@`; **Peter skips clearly-personal mail** (charter §3); the ~5% personal in `minda@` is **migrating to Minda's separate personal inbox** over time (residual → zero). Recorded the owner **delivery convention** (charter §2b: routine prompts/instruction sets handed over as complete paste-ready replacements, never deltas) and **filed Peter's canonical inbox routine prompt** (`Peter - AI Data Assistant/Routine-Prompt-Inbox-Triage.md`, reads `ops@` = info@ + business-minda@, skip-personal). No live-system change made by Eugene (read-only verification only). **Later still 2026-09-13, built the Construction email architecture — invoice@ AP inbox, Dext bookkeeping, sent-mail routing, and Google Vault evidence retention.** Added `invoice@fishboneconstruction.co.uk` (accounts-payable inbox; alias `invoice@fishbonedrylining.co.uk` reaches the same mailbox) as a third forwarded source into `ops@` (verified 2026-09-13), and updated Peter's inbox-triage prompt to **v2026-09-13c** with an INVOICES block (capture supplier/amount/number/due date, flag deadlines; never pay, approve, or act on bank-detail changes — human-only, fraud-risk flagged) and an outbound/own-sent-mail handling rule. **Dext bookkeeping** (`invoice@` → `mindaugas.gaudiesius@dext.cc` → QuickBooks) set up at the **mailbox level, deliberately NOT via Peter** — forwarding + writing to a system of record breaches Peter's boundary (charter §3) and Dext auto-posts, so the mailbox forwards and Peter stays an independent read-only control check; every invoice reaches both Dext and `ops@`. **Sent-mail routing:** an Admin-console routing rule ("Ops routing", Gmail → Routing) copies outbound + internal-sending mail from minda@/info@/invoice@ into `ops@`, so Peter sees both sides of a thread (test from minda@ to an external address verified in `ops@` 2026-09-13). **Google Vault evidence retention:** because the `ops@` forwards are inbound-only, sent mail (often the evidence) is captured by Vault instead — Gmail **default retention set to Indefinitely / No expiry** (a Vault licence alone is not retention; licences held for minda@/info@/invoice@); Vault is the evidence store, Peter never reads it. New runbook `Runbooks/Runbook-Email-Evidence-Retention-Google-Vault.md` (v0.2); CH-access runbook to **v0.2** (the environment-credential mechanism); inventory updated with the mail-flow/retention section, the identity table and the invoice@ row. All guide-only for Eugene; Minda executed every console step (Dext filter, Vault retention rule, routing rule); no secret stored. **2026-09-12 — created.** Eugene stood up as the group's second AI employee (after Peter), built before Content & Marketing at Minda's instruction. Drive home `My Drive / Eugene - AI IT Assistant` (`1o4MBRcckZBspw-uT6qM2V-74H6OsRK9T`) with `Runbooks/`, `Infra-Inventory/`, `Hardware-Projects/`, `change-log/`, `Archive/`. Charter `CLAUDE.md`, `README.md` and the four control files uploaded (byte-verified). Git mirror `minda-ui/Eugene` seeded (charter, README, control files, the group-standard SessionStart PDF-toolkit hook). Added to the Fishbone Group master index (`CLAUDE.md` §1 sister systems) and noted in the AI Workforce Plan as the IT/ops build, promoted to build #2. No live-system change made and no routine created — Eugene is interactive by default (§5). |
| Last session by | Claude (AI assistant), on behalf of minda@fishboneconstruction.co.uk |
| Role | IT & engineering enablement. Writes runbooks/config/code and KB scaffolding; **edits code/repos/KB directly, guide-only for live systems, never holds secrets** (charter §3). |
| Beats | 2a software-setup enablement (runbooks + config + verification); 2b build/maintain workforce infrastructure; 2c hardware/automation coding; 2d infra inventory + IT change-log; 2e AI Workforce Hub — reads Roster/Tasks/Achievements, updates own Tasks & Requests rows, manages Help & Lessons (added 2026-09-14, reconciled into this KB 2026-09-16). |
| Connectors | Google Drive, GitHub, Web, **Smartsheet** (the AI Workforce Hub, charter §2e — confirmed account-wide 2026-09-16, though session-scoped like GitHub; see AWT-0012/HL-0008). **No Gmail** (Eugene is not an email agent). |
| Routines | **None.** Interactive by default. A light read-only weekly health-check routine is optional-later (§5), to be created via the routines form once the infrastructure has stabilised. |
| Items done | 0 IT tasks logged yet (creation itself is this session's change-log entry, not a ledger row). See `processed-items-ledger.md`. |
| Open issues | 4 open + 2 resolved. **OI-1 resolved 2026-09-13** (separate subscriptions; Commercial rides Properties). **OI-4 resolved 2026-09-13** (Construction-hub partial consolidation; Properties + Amfa standalone; updated 2026-09-15 — Holdings done and MX-verified, SSAS dropped). Open: **OI-2** (Gmail connector delegated-mailbox capability — moot for Construction), **OI-3** (first-runbooks priority — largely settled by OI-4), **OI-5** (2026-09-15, advisory/not blocking — super-admin logins are shared business mailboxes, not dedicated admin accounts), **OI-6** (2026-09-15, task/not blocking — Holdings' SPF record still points at IONOS, needs `include:_spf.google.com` added in the 1&1 panel). See `open-issues.md`. |
| Next action | **(1) Companies House access** `Runbooks/Runbook-Companies-House-Access-for-Peter.md` v0.2 — **RESOLVED end-to-end 2026-09-13 (Peter OI-6 closed):** the gov.uk egress block is the **organisation's network egress policy** (not a routine/connector/allowlist toggle available to the user), so the fix is an **environment API credential** — a **Basic** credential ("Companies House API", allowed website `api.company-information.service.gov.uk`, free CH REST API key as username, blank password) the proxy attaches automatically while keeping the key **write-only** (Eugene/Peter never hold it). Credential added to **both** the "Fishbone Group" and the **"Minda"** environment (where Peter's routines run); Peter's weekly beat-2b run then **succeeded on its fourth attempt** — all six companies + the trustee watch returned HTTP 200. Recommend the owner confirm the Minda-env credential is durable (not silently reverted) before relying on the unattended weekly run. **(2) Email architecture — DONE 2026-09-13:** dedicated `info@` + `ops@` (Peter OI-5 closed); invoice@ AP inbox forwarding into `ops@`; Dext (`invoice@` → dext.cc → QuickBooks) at the mailbox level; sent-mail routing into `ops@`; Google Vault retention set to Indefinitely. Both of Peter's routine prompts (inbox-triage v13c, Companies House) pasted into the routines form by Minda. Nothing outstanding here. **(3) Workspace consolidation** `Runbooks/Runbook-Workspace-Consolidation-into-Construction.md` (now **v0.4, "ready to execute"** — Holdings done and **MX-verified** 2026-09-15, SSAS dropped) — **§4 (Waste) is fully decided end-to-end, nothing left open. Standalone execution checklist:** `Runbooks/Runbook-Waste-Migration-Execution-Checklist.md` (v1.0) — Phase A archive into Waste's own KB `Fishbone Waste Ltd - Knowledge Base/Raw/` (SRC-9, per group §7a) → Phase B confirm Construction headroom → Phase C free the domain from Waste's Workspace (**flagged open question:** primary vs secondary domain — check the console; primary likely means full subscription cancellation, not a simple removal; DNS-host confirmed at 1&1, no registrar-transfer issue) → Phase D add as secondary domain to Construction (**DNS wrinkle:** enter the Google verification TXT in **WordPress's DNS dashboard**, not IONOS — the domain's real nameservers point at WordPress, IONOS's panel is inactive) → Phase E recreate both mailboxes → Phase F optional mail restore (must happen before Phase C if wanted) → Phase G cut MX **and fix SPF together, also in WordPress's DNS** (don't repeat the Holdings/OI-6 gap) → Phase H cancel Waste subscription → Phase I verify. `sales@`'s super-admin role resolves itself at cancellation, no action needed. Separately, low-urgency: fix Holdings' stale SPF record (still points at IONOS, not Google) in the 1&1 panel, tracked as **OI-6**. OI-5 (other subscriptions' shared-mailbox admin logins) remains open, advisory only. All guide-only: Minda executes, Eugene verifies. (4) When Content & Marketing is built (AI Workforce Plan v2 Phase 2), Eugene scaffolds its KB + drafts its routine prompt. |
